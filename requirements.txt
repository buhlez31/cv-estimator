anthropic>=0.40.0
streamlit>=1.32.0
pydantic>=2.5.0
pypdf>=4.2.0
python-docx>=1.1.0
plotly>=5.18.0
pandas>=2.1.0
python-dotenv>=1.0.0
# Optional Layer C — live job postings via Apify abaddion/jobscz-scraper.
# Module degrades to None when APIFY_TOKEN is unset, so this dep is required
# only when the live-postings panel is wanted.
apify-client>=1.7.0
# Install the local cv_estimator package itself. Streamlit Cloud only
# runs `pip install -r requirements.txt`; without this line, imports like
# `from cv_estimator.models import ...` fail at startup.
.
