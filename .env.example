ANTHROPIC_API_KEY=your_key_here

# Optional — when set, the pipeline cross-checks the ISPV salary band against
# live jobs.cz postings (Apify actor abaddion/jobscz-scraper). Skip this if
# you don't want the third salary layer; pipeline falls back to ISPV + region.
APIFY_TOKEN=
